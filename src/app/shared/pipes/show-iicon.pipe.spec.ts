import { ShowIiconPipe } from './show-iicon.pipe';

describe('ShowIiconPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowIiconPipe();
    expect(pipe).toBeTruthy();
  });
});
